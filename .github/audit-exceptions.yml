version: 1
exceptions:
  - package: xlsx
    advisory: "GHSA-4r6h-8v6p-xvw6"
    severity: high
    reason: "Admin export only; switched to dynamic import to reduce exposure (CVE-2023-30533)"
    mitigation: "Load only on export; restrict export permissions and data scope"
    expires_on: "2026-10-06"
    owner: "security@your-domain"
  - package: xlsx
    advisory: "GHSA-5pgg-2g8v-p4x9"
    severity: high
    reason: "Admin export only; switched to dynamic import to reduce exposure (CVE-2024-22363)"
    mitigation: "Load only on export; restrict export permissions and data scope"
    expires_on: "2026-10-06"
    owner: "security@your-domain"
  - package: lodash
    advisory: "GHSA-r5fr-rjxr-66jc"
    severity: high
    reason: "lodash _.template not used with untrusted input; only internal admin UI templates"
    mitigation: "No user-controlled template strings; plan to migrate to lodash-es tree-shaken imports"
    expires_on: "2026-07-02"
    owner: "security@your-domain"
  - package: lodash-es
    advisory: "GHSA-r5fr-rjxr-66jc"
    severity: high
    reason: "lodash-es _.template not used with untrusted input; only internal admin UI templates"
    mitigation: "No user-controlled template strings; plan to migrate to native JS alternatives"
    expires_on: "2026-07-02"
    owner: "security@your-domain"
  - package: axios
    advisory: "GHSA-3p68-rc4w-qgx5"
    severity: critical
    reason: "NO_PROXY bypass not exploitable; all API calls go to known endpoints via server-side proxy"
    mitigation: "Proxy configuration not user-controlled; upgrade when axios releases fix"
    expires_on: "2026-07-10"
    owner: "security@your-domain"
  - package: nanoid
    advisory: "GHSA-2v37-7h3g-55p8"
    severity: high
    reason: "Custom generator with size=0 not used; all nanoid calls use default size or positive values"
    mitigation: "No zero-size custom generators in codebase; upgrade when nanoid releases fix"
    expires_on: "2026-10-13"
    owner: "security@your-domain"
  - package: nanoid
    advisory: "GHSA-28wg-ghj8-5hjv"
    severity: high
    reason: "nanoid not imported in frontend app code; transitive build-time dep (vite/vitest). No custom non-secure generator with negative size is used."
    mitigation: "Build-time only; no untrusted size input; upgrade when nanoid releases a fix"
    expires_on: "2026-12-06"
    owner: "security@your-domain"
  - package: nanoid
    advisory: "GHSA-xwg4-73v4-xw9w"
    severity: high
    reason: "nanoid not imported in frontend app code; transitive build-time dep (vite/vitest). Default/standard sizes only; no untrusted size that could trigger integer overflow."
    mitigation: "Build-time only; no untrusted size input; upgrade when nanoid releases a fix"
    expires_on: "2026-12-06"
    owner: "security@your-domain"
  - package: postcss
    advisory: "GHSA-r28c-9q8g-f849"
    severity: high
    reason: "postcss runs at build time only (vite CSS pipeline) on trusted project sources; no user-controlled sourceMappingURL in the build. Not present in the production runtime bundle."
    mitigation: "Build-time only on trusted input; upgrade postcss when a fix is available"
    expires_on: "2026-12-06"
    owner: "security@your-domain"
  - package: axios
    advisory: "GHSA-gcfj-64vw-6mp9"
    severity: high
    reason: "Frontend axios runs in the browser (XHR/fetch adapter); the advisory affects only the Node HTTP adapter inherited proxy after interceptor config cloning. Browser adapter has no proxy concept, so not exploitable."
    mitigation: "Browser-only usage; no Node HTTP adapter; upgrade axios when a fix is available"
    expires_on: "2026-12-06"
    owner: "security@your-domain"
